---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contact" description="Get in touch with Bearing Freedom.">
  <!-- Hero -->
  <div class="bg-navy">
    <div class="max-w-4xl mx-auto px-5 lg:px-8 pt-12 pb-16">
      <div class="text-xs text-gold font-semibold tracking-[0.2em] uppercase mb-2 animate-fade-up">Reach Out</div>
      <h1 class="font-[Playfair_Display] text-3xl font-bold text-white tracking-tight mb-3 animate-fade-up stagger-1">Get in touch</h1>
      <p class="text-white/50 text-sm font-[Newsreader] animate-fade-up stagger-2">Have a question, tip, or want to collaborate? Reach out below.</p>
    </div>
  </div>

  <!-- Contact Form -->
  <div class="max-w-2xl mx-auto px-5 lg:px-8 py-14">
    <form
      id="contact-form"
      name="contact"
      method="POST"
      data-netlify="true"
      class="space-y-6"
    >
      <input type="hidden" name="form-name" value="contact" />

      <div>
        <label for="contact-name" class="block text-sm font-semibold text-navy mb-1.5">Name</label>
        <input
          type="text"
          id="contact-name"
          name="name"
          required
          class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm text-navy placeholder:text-gray-warm/50 focus:outline-none focus:border-gold focus:ring-2 focus:ring-gold/15 transition-all"
          placeholder="Your name"
        />
      </div>

      <div>
        <label for="contact-email" class="block text-sm font-semibold text-navy mb-1.5">Email</label>
        <input
          type="email"
          id="contact-email"
          name="email"
          required
          class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm text-navy placeholder:text-gray-warm/50 focus:outline-none focus:border-gold focus:ring-2 focus:ring-gold/15 transition-all"
          placeholder="you@example.com"
        />
      </div>

      <div>
        <label for="contact-subject" class="block text-sm font-semibold text-navy mb-1.5">Subject</label>
        <input
          type="text"
          id="contact-subject"
          name="subject"
          required
          class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm text-navy placeholder:text-gray-warm/50 focus:outline-none focus:border-gold focus:ring-2 focus:ring-gold/15 transition-all"
          placeholder="What is this about?"
        />
      </div>

      <div>
        <label for="contact-message" class="block text-sm font-semibold text-navy mb-1.5">Message</label>
        <textarea
          id="contact-message"
          name="message"
          required
          rows="6"
          class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm text-navy placeholder:text-gray-warm/50 focus:outline-none focus:border-gold focus:ring-2 focus:ring-gold/15 transition-all resize-none"
          placeholder="Your message..."
        ></textarea>
      </div>

      <button
        type="submit"
        id="contact-submit"
        class="bg-gradient-to-r from-gold-dark to-gold text-navy px-8 py-3 rounded-lg text-sm font-semibold hover:shadow-xl hover:shadow-gold/20 transition-all hover:scale-[1.02] active:scale-[0.98]"
      >
        Send Message
      </button>
    </form>

    <!-- Success message (hidden by default) -->
    <div id="contact-success" class="hidden mt-8">
      <div class="inline-flex items-center gap-2 bg-gold/15 text-gold-dark px-5 py-3 rounded-lg text-sm font-medium">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M5 13l4 4L19 7"/></svg>
        Message sent. Thanks for reaching out!
      </div>
    </div>
  </div>

  <script is:inline>
    document.getElementById('contact-form')?.addEventListener('submit', async function(e) {
      e.preventDefault();
      const btn = document.getElementById('contact-submit');
      const success = document.getElementById('contact-success');

      btn.textContent = 'Sending...';
      btn.disabled = true;

      try {
        await fetch('/', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams(new FormData(this)).toString(),
        });
        this.style.display = 'none';
        success.classList.remove('hidden');
        success.classList.add('animate-fade-up');
      } catch {
        btn.textContent = 'Try Again';
        btn.disabled = false;
      }
    });
  </script>
</BaseLayout>
